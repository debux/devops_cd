name: DemoCD
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
#      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo Hello, world!

  deploy-dev:
    runs-on: self-hosted
    environment:
      name: dev
      url: 'http://www.google.com'
    needs: build
    steps:
#      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo Deploy Dev
        
  deploy-hmg:
    runs-on: self-hosted
    environment:
      name: hmg
      url: 'http://www.google.com'
    needs: deploy-dev
    steps:
#      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo Deploy HMG
  
  deploy-prd:
    runs-on: self-hosted
    environment:
      name: prd
      url: 'http://www.google.com'
    needs: deploy-hmg
    steps:
#      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo Deploy PRD
